python-microgrid==1.4.1

# python-microgrid 1.4.1 calls np.product, removed in NumPy 2.0, so the
# scientific stack is pinned to the NumPy 1.x era it was built against.
numpy==1.26.4
pandas==2.1.4
scipy==1.11.4
matplotlib==3.8.4
cvxpy==1.4.3

# The interactive console (scripts/serve.py). Not needed for the CLI scripts.
fastapi==0.115.6
uvicorn==0.34.0
