# Copy this file to .env and paste your own key. .env is gitignored; never commit it.
#
# Only the farmer-advice translation needs this. Everything else -- simulation, sizing,
# the optimiser, the console, the deterministic English briefing -- runs without it.
#
# A key is free: https://aistudio.google.com/apikey  (sign in, "Create API key", copy)
GEMINI_API_KEY=
